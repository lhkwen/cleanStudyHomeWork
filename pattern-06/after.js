// 변경 후: 의도만 남기고 타입/접두 제거
const userName = "Alice";
let retryCount = 3;
class ShapeFactory {
  createCircle(){ /* ... */ }
}
class DefaultShapeFactory extends ShapeFactory {
  createCircle(){ /* ... */ }
}
